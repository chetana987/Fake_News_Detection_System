spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/verification_dev
    username: postgres
    password: postgres
  kafka:
    bootstrap-servers: localhost:9092
  jpa:
    show-sql: true

logging:
  level:
    com.misinfo: DEBUG

api:
  key: dev-api-key-123

cors:
  allowed:
    origins: http://localhost:5173,http://localhost:3000
