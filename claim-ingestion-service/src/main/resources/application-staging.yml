spring:
  datasource:
    url: jdbc:postgresql://staging-db:5432/claim_ingestion
    username: ${DB_USER}
    password: ${DB_PASS}
  kafka:
    bootstrap-servers: staging-kafka:9092

logging:
  level:
    com.misinfo: INFO

api:
  key: ${API_KEY}

cors:
  allowed:
    origins: https://staging.misinfo.app
